# Database configuration
DB_HOST=localhost
DB_NAME=gamefleet_db
DB_USER=gamefleet_user
DB_PASSWORD=changeme
DB_PORT=5432

# Production deployment ports
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Frontend server options: node-server or nginx-server
FRONTEND_TARGET=node-server